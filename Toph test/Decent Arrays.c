#include<stdio.h>
int main()
{
    int n,a[100],i;

    printf("Enter how many number : ");
    scanf("%d",&n);

    for (i=0;i<n;i++);
    {
       // printf("a[i] = ");
        scanf("%d",&a[i]);
    }
}
