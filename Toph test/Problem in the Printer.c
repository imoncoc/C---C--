#include<stdio.h>
#include<string.h>
int main()
{
    char str[100],ch, end;
    int i;
/*gets(str);
    printf("%s",str);*/

    while (end!='\0')
    {
        for(i=0;i<end;i++)
        {
           gets(str);
        }

    }


    return 0;
}
